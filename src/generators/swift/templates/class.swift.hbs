/**
 * {{autogeneratedFileWarning}}
 */

import Foundation

public class {{name}}: TypewriterSerializable {
{{#each properties}}
{{#if description}}
/// {{description}}
{{/if}}
    public var {{name}}: {{type}}{{#isVariableNullable}}?{{/isVariableNullable}}
{{/each}}

    public {{functionSignature "init" properties false}} {
        {{#each properties}}
        self.{{name}} = {{name}}
        {{/each}}
    }

    public func serializableDictionary() -> [String: Any] {
        {{indent "        " (propertiesDictionary properties "self.")}}

        return properties;
    }
}